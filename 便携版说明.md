# 便携版使用说明

本项目现为 Windows x64 便携整合包。Python、Node.js、FFmpeg、官方 IndexTTS2、Faster-Whisper 及其 Python 依赖均位于项目目录内，不依赖系统 Python、Conda、Node.js 或 CUDA Toolkit。

## 启动

- 双击 `start_windows.bat`：启动完整工作台。
- 双击 `start_backend_dev.bat`：只启动后端。
- 双击 `start_indextts2_ui.bat`：单独启动官方 IndexTTS2 WebUI。
- 双击 `stop_dev.bat`：停止本项目启动的 Python/Node 服务。
- 双击 `0_一键初始化环境.bat`：检查便携 Python，并在 Node 依赖缺失时安装到本项目。

## 搬迁或打包

必须整体复制项目文件夹，尤其不要遗漏：

- `runtime/python`：便携 Python 3.10 和全部 Python 依赖。
- `runtime/node`：便携 Node.js/npm。
- `tools/IndexTTS2/checkpoints`：IndexTTS2 模型。
- `tools/ffmpeg`：FFmpeg。
- 根目录和 `frontend` 下的 `node_modules`。

所有程序路径均由当前项目目录动态计算，移动或改名后无需修改 `.env`。运行缓存会自动写入 `runtime/cache`、`runtime/temp` 和 `runtime/data`。

## 系统要求与注意事项

- 仍需 Windows 10/11 x64 和兼容的 NVIDIA 显卡驱动；显卡驱动属于系统组件，不能随普通整合包携带。
- 项目已经自带 PyTorch CUDA 12.8 运行库，不要求另装 CUDA Toolkit。
- `.env` 含接口密钥和本地配置。对外分发前请替换为 `.env.example`，不要公开自己的密钥。
- 当前交付形态是可直接运行的便携整合包，不是单文件 EXE。后续可在此基础上制作启动器或安装包。
