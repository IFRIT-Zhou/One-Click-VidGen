# One-Click VidGen 使用说明

版本日期：2026-07-31

## 1. 这是什么

One-Click VidGen（界面名称“一键成片”）可以把一篇文案自动制作成带配音、画面、字幕和 BGM 的横版视频。你也可以只用它配音、识别字幕，或者在成片后重新绘图、调整画面时序和局部重配音。

## 2. 使用前准备

### 整合包用户

1. 把压缩包完整解压到空间充足的磁盘，建议使用短路径，例如 `D:\One-Click-VidGen`。
2. 不要直接在压缩软件预览窗口内运行。
3. 双击 `start_windows.bat`。
4. 等待黑色启动窗口显示服务已启动，浏览器会自动打开工作台。
5. 第一次使用时点击左侧“运行环境检测”。

请保留启动黑窗口。正常关闭它时，程序会尝试一并关闭本次前后端服务。

### Git 源码用户

Git 仓库主要包含源码、配置示例和依赖清单，不保证包含完整模型、CUDA、Python、Node、FFmpeg 和便携浏览器。请按仓库 README 安装依赖并复制 `.env.example` 为 `.env`。不熟悉环境配置的用户建议使用完整整合包。

## 3. 配置 API Key

登录后在左侧配置模型接口：

- 语言模型：用于 Agent 0/1/2、无原稿字幕校正。可选择 Gemini、第三方兼容接口、DeepSeek、OpenAI、Kimi 或 GLM。
- 图像模型：用于 Image2 生图和重绘。
- 通用 API Key：当语言和图像 Key 都没单独填写时，可作为共用 Key。
- Qwen-TTS Key：只有选择 Qwen-TTS 云配音时需要。

填写后点击保存。成功时输入框会变为绿色“API 已配置”；点击右侧铅笔可重新输入。图像和通用接口右侧的加号可以增加更多账号，用于并行出图。

如果状态变红并显示 ERROR，通常是 Key 无效、余额不足、网络超时或接口配置错误。修改 Key 后重新保存即可。

## 4. 最简单的一键生成方法

1. 打开“生成工作台”。
2. 填写项目名称。请使用容易识别的名称；程序不会在开始后改成随机名。
3. 上传 TXT/Markdown 文案，或直接粘贴到“口播文案”。
4. 选择配音：
   - IndexTTS2：上传 10–30 秒、单人、清晰、无背景音乐的参考音频；
   - Qwen-TTS：选择官方音色，并填写简短、明确的配音描述。
5. 选择作品风格：都市惊悚、口播科普、纯科普、通用自定义或自己的 Agent 预设。纯科普适合不需要固定主持人的生物、医学、物理、化学、数学、天文地学、工程计算机或人文社科内容。
6. 检查统一画风、全局人物和故事世界设定。
7. 如需角色参考图，最多上传三张，并在人物说明中写清“男主参考图1、女主参考图2”等绑定关系。
8. 如需 BGM，打开开关并上传音乐。
9. 选择成片版本：字幕版、纯净版或双版本。
10. 点击右上角明亮蓝色“一键生成视频”。

任务运行时不要反复点击启动。可以切到其他窗口，完成后浏览器会尝试播放提示音并提醒你回来查看。

## 5. 文案和画面建议

- 单次正文不要超过约 12,000 字符；更长内容建议按章节分别生成，后期再拼接。
- 1,000–3,000 字的文案最适合快速测试完整流程。
- 人物设定尽量写稳定特征和阶段变化，例如“前期灰色旧衣，买装备后改为白色骑行服”，不要只写“漂亮女人”。
- 画风要明确媒介，例如“半写实厚涂插画”“二维绘本插画”；如果只写“真实生活场景”，模型可能输出摄影写实。
- 参考图只在确实需要角色或物品一致性时上传。参考图会提高接口消耗。
- 手机、电脑内容只有在正文明确给出具体消息、照片或页面时才适合画屏幕特写；否则系统会优先表现人物正在使用设备。
- 明确设备内容时，系统会守住原文核心事实，但允许模型自然设计状态栏、列表、图标、色块和纸面版式，不要求机械照抄整段原文。

## 6. IndexTTS2 参数建议

- 参考音频：10–30 秒，单人，无混响，无背景音乐。
- 4090 常用并行数：2。
- 其他显卡或显存不足时：先使用并行 1。
- 并行 3 属于上限尝试档，可能因句长、音色和后台显存占用而溢出。
- 如果显存不足，请停止任务，关闭其他占用 GPU 的程序，重启本地服务后降低并行数。

模型首次加载可能需要几分钟。日志会在生成临时句子音频后显示真实完成句数。

## 7. 使用已有配音

如果已经有配音和文案：

1. 勾选“已有配音和文案，不需要 IndexTTS2”。
2. 上传配音和对应文案。
3. 系统会跳过 TTS，从字幕识别、校对和分镜继续。

如果只有配音没有原稿，也可以运行；系统会把 ASR 识别结果作为全文资料，但专有名词准确率和 Agent 理解效果通常不如同时提供原文。

## 8. 分步模式

顶部打开“分步模式”后，任务会分三段：

1. 配音完成后暂停：试听完整配音。不满意可点击重新配音；满意后确认并开始配图。
2. 图片完成后暂停：检查画面或进入画面修改；满意后确认并渲染。
3. 视频渲染和归档。

分步模式适合正式作品，可以在昂贵的出图和渲染前及时发现配音问题。

## 9. BGM 使用

打开“添加 BGM”后：

- 可连续上传多首音乐；
- 播放按钮可试听；
- 上下箭头调整顺序；
- 每首可单独设置音量，默认值适合做背景声，仍应以试听为准；
- 音乐按列表顺序播放，全部播完后从第一首重新循环；
- 开启渐弱后，可设置切歌和结尾渐弱时长；
- “清空列表”会移除本次设置中的全部 BGM。

如果成片后才发现忘记加 BGM，可在画面修改项目下方的“更改项目 BGM”中补加，然后重新渲染，无需重新出图。

## 10. 保存和读取参数

右上角可以保存当前参数。保存内容包括项目相关输入、画风、人物、节奏、TTS 选项、正文和其他 UI 参数。保存名称默认与项目名关联。

以后可从下拉框选择预设并点击“读取参数”。不再需要的旧预设可以删除。读取后仍应检查本地参考音频、参考图和 BGM 是否存在，因为外部文件被移动后无法自动恢复。

## 11. 画面修改

完成项目后展开“画面修改”，从 `output` 中选择当前或历史项目。

每张画面提供：

- 点击图片：放大查看；
- ▶：使用当前输入框提示词重绘；
- 撤回：恢复上一版图；
- 重置：恢复初始提示词；
- 替换：上传本地图片替代当前图；
- 参考图：为这次重绘选择最多三张本地参考图；
- ↑：把当前画面作为本次重绘的图1；
- ✅：把满意的当前图确认成新的默认基线。

当你在另一张卡片选择参考图时，前一张卡片的临时参考选择会清空，防止串图。多张重绘可以同时提交，系统会尽量使用多个图像账号并行。

全部图片满意后可“确认全部当前画面”，再点击重新渲染。

## 12. 画面时序

画面时序区用于解决画面与字幕略微错位：

- 调整一张画面覆盖的字幕开始和结束位置；
- 让某张画面多持续一句或少持续一句；
- 删除多余画面，系统会把其字幕尽量分给相邻画面；
- 保存当前时序；
- 恢复某个历史时序；
- 恢复最初时序。

修改完必须重新渲染才会生成新视频。建议每次大改前先保存当前时序。

## 13. 配音精修

在画面时序下方可以逐句试听原始 TTS：

1. 点击句子卡片的播放按钮试听。
2. 勾选一条或多条需要重配的句子。
3. 点击重配音。
4. 生成完成后再次试听新结果。

系统会根据新句子的实际时长重新拼接整条配音，并同步更新字幕和画面时间线。重配后仍建议从前后各听一遍，确认衔接自然，再重新渲染。

## 14. 单独模块 1：仅配音

顶部打开“模块 1·仅配音”，上传文案和参考音色后即可只运行 TTS。产物放在项目根目录的 `TTS_Output` 中，不会混入普通视频项目 output。

## 15. 单独模块 2：字幕识别

打开“模块 2·字幕识别”：

1. 上传常见音频或视频文件。
2. 选择是否校对。
3. 有原文时上传文档，系统按原文校对；无原文时可使用语言模型校对。
4. 任务完成后在日志下方下载 SRT 或打开输出目录。

输出位于 `output/字幕任务ID`。

需要直接得到带字幕视频时，可打开“启动字幕添加”，选择字幕样式和系统字体后渲染。BGM 是独立开关，不依赖字幕添加开关。

## 16. 断点续跑、停止和删除

- 断点续跑：任务失败后优先使用，系统会检查已有音频、字幕和图片，只补缺失阶段。
- 停止生成：发送安全停止请求。TTS 正在 GPU 推理时可能需要短暂等待清理。
- 删除任务：删除任务列表及其临时 workspace 数据。正式 output 项目是否保留以界面提示为准，删除前请先确认。
- 不要直接手动删除正在运行任务的 workspace。

## 17. 输出目录

普通视频项目：

```text
output/项目名称/
├─ input/   原始输入和归档素材
├─ image/   当前使用的画面
├─ video/   最终视频
└─ other/   SRT、提示词、映射、时间线和编辑数据
```

点击“打开项目输出文件夹”可由后端调用 Windows 资源管理器打开相应目录。

## 18. 常见问题

### 一直显示模型准备中

IndexTTS2 首次加载较慢。先观察任务管理器显存和日志；生成第一条临时 WAV 后会出现真实句数进度。若十几分钟完全无变化，导出诊断包再排查。

### CUDA out of memory

降低并行数为 1，关闭游戏、浏览器硬件加速或其他 GPU 程序，重启服务后再试。不要在显存溢出后连续点击多个新任务。

### 图像账号余额不足

补充余额或通过加号添加其他账号。系统会尽量跳过明确失败的账号，但所有账号都不可用时任务仍会失败。

### 某一张图失败

先等待自动重试。整个任务失败后点断点续跑；成片后也可在画面修改区单独重绘该图。

### Agent 0、Agent 1 或 Agent 2 规划失败

语言模型是出图前的关键步骤。Key 未配置、余额/限流、HTTP 502、上游超时、空响应或 JSON 异常时，系统会在提交 Image2 前停止，避免用低质量兜底提示词继续花费图像积分。已经生成的配音和字幕会保留；检查语言模型 Key、余额和服务状态后，重启已更新的程序并点击断点续跑即可。仅刷新浏览器不会加载新的 Python 后端代码。

### 画面与字幕对不上

先确认是否是长文旧项目。新版任务可在画面时序中直接调整覆盖字幕，然后重渲染。

### 点击按钮像没有反应

先看主日志和按钮旁的状态。如果刚更新过补丁，关闭启动窗口并重新双击启动，防止浏览器还连着旧后台。仍异常时导出问题诊断包。

## 19. 提交问题时请提供

- 项目版本或 Git 提交号；
- 操作系统和显卡型号/显存；
- 使用的 TTS、语言模型和图像接口类型；
- 出错的大概时间和阶段；
- 日志末尾 50–100 行；
- “导出问题诊断包”生成的压缩文件；
- 如涉及画面错位，提供项目名、画面编号和对应字幕。

请不要在聊天、Issue 或诊断包中公开 API Key、私人音频和未授权素材。
