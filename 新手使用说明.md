# One-Click VidGen 新手使用说明

## 首次使用

1. 将整个文件夹解压到一个纯英文、无特殊符号的本地路径，例如 `D:\One-Click-VidGen`。
2. 在根目录复制 `.env.example` 并重命名为 `.env`。
3. 用记事本打开 `.env`，填写自己的 Gemini 和 RunningHub / Image2 接口配置；不要把 `.env` 发给任何人或提交到 Git。
4. 双击根目录的 `双击启动.bat`，浏览器会自动打开程序页面。
5. 在“创建故事视频”中输入文案、上传自己的参考音频，按需选择作品模式与画面风格后开始生成。

## 运行要求

- Windows 10 / 11 64 位。
- 建议使用 NVIDIA 显卡；IndexTTS2 配音会使用 CUDA GPU。显存不足时请降低配音并行数。
- 首次启动不需要在系统盘安装 Python、Node.js 或 FFmpeg：它们已随整合包放在项目目录内。
- 请预留至少 30 GB 可用空间，用于任务图片、音频和视频输出。

## 文件说明

- `双击启动.bat`：普通用户只需双击它启动。
- `output`：每个任务的最终视频和可编辑资料。
- `workspace`：任务运行中的临时资料；不要手动删除正在运行任务的文件。
- `runtime_logs`：启动或任务出错时用于排查的日志。

## 常见问题

- 启动后提示缺少 `.env`：按上方第 2、3 步创建并填写它。
- 浏览器没有自动打开：手动访问 `http://127.0.0.1:5173`。
- 端口被占用或页面异常：关闭旧的命令窗口后重新双击启动脚本。

本整合包不包含任何个人 API Key、个人音色、历史文案或生成结果。
