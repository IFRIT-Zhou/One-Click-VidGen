# One-Click VidGen：Codex 项目记忆备份

用途：把本文件作为附件或直接交给新的 Codex 项目/新对话，使其快速理解项目背景、当前架构、用户偏好和不可破坏的约束。

更新时间：2026-07-31

## 1. 项目身份

- 项目中文名：一键成片。
- 英文名：One-Click VidGen。
- 本地根目录通常为 `G:\trae_voice_over_video`。
- GitHub 主仓库当前为 `https://github.com/IFRIT-Zhou/One-Click-VidGen.git`。
- 项目发起和主要产品设计者为用户；早期前端/登录有合作者基础，其余大量功能由用户持续设计、测试并与 AI 协作实现。
- 目标是免费开源核心程序，同时保留第三方 API 代购协助和云端 TTS 算力服务等潜在商业化空间。

## 2. 用户和协作风格

- 用户是药学背景，不以手写代码为主要能力，但对产品流程、用户体验、视频效果和边界情况非常敏感。
- 用户希望先讨论高风险架构改动，再施工；对明确的小功能和 Bug 修复倾向于直接实现、测试、交付。
- 回答应先给结论，用清楚的产品语言解释，不要只报代码名。
- 用户会实机运行长任务，应避免随意重启、清理 workspace、覆盖 output 或破坏正在重绘的结果。
- 修改前先检查当前任务是否运行；可能影响任务时，应说明风险或等任务结束。
- 用户重视 token 成本：语言 Agent 不应读取参考图像像素，只读取“角色对应图1/图2/图3”的文字绑定。
- 用户要求便携化：不能写死盘符、用户名或 C 盘缓存路径；模型、环境和依赖应尽可能在整合包内相对定位。

## 3. 产品现状

这是一个 Vue + FastAPI 的本地工作台，可完成：

1. 文案输入或上传；
2. IndexTTS2 本地配音或 Qwen-TTS 云配音；
3. Faster-Whisper 字幕识别和原文校正；
4. Agent 0/1/2 语义理解与镜头提示词；
5. Image2 第三方接口多账号生图；
6. FFmpeg/Hyperframes 视频渲染；
7. BGM、字幕版/纯净版输出；
8. 画面重绘、时序调整、配音精修和后期重渲染。

支持都市惊悚、口播科普、纯科普、通用自定义和完全自定义 Agent 预设。纯科普覆盖多学科严肃知识可视化，默认无固定主持人物。支持普通一键模式和分步审核模式。

## 4. 当前正确流水线

必须牢记当前顺序：

```text
输入文案/已有配音
→ 模块1 TTS（可跳过）
→ 模块2 Faster-Whisper 时间戳
→ 模块2.5 原文校对
→ Agent 0 阅读全文，建立全局资料
→ Agent 1 阅读完整字幕时间线，划分语义事件
→ 长文按 Agent 1 边界拆为渲染段
→ Python 将语义事件装配为固定画面分组
→ Agent 2 为固定分组写生图提示词
→ Python 角色/参考图/设备/媒介保护
→ Image2 生图
→ 模块5 渲染、字幕、BGM
→ output 归档
→ 后期编辑和重渲染
```

严禁重新退回“Python 先按最短秒数切死，再让 Agent 理解”的旧思路。当前原则是语义正确优先于最短画面时长。

## 5. Agent 职责和版本

截至 2026-07-31：

- `STORY_AGENT_VERSION = 10`
- `STORY_CONTEXT_VERSION = 4`
- `CHARACTER_CONTINUITY_VERSION = 4`
- `VISUAL_PROMPT_AGENT_VERSION = 15`

Agent 0 输出全文资料：人物 ID、服装阶段、地点、世界背景、关键物件、屏幕信息对象、线索与连续性。

Agent 1 输出带 slide 边界的语义单元：事件、视觉焦点、角色、节奏、hard/soft 边界、设备镜头三态。Agent 1 需要时间戳。

Python 分组器只做确定性校验和装配，不假装理解故事。必须检查全覆盖、连续、无重叠，并执行 Agent 1 的事件边界。

Agent 2 只能为 Python 固定的 `includes_slides` 写 `image_prompt`，不能重新分组。协议和输出 JSON 栏属于灰色不可编辑部分；专家模式只开放创作指令。

所有模式的人物资料后都应包含：当画面中没有这些角色时，本段不作为参考。最终 image prompt 不应机械重复整份人物资料。

生产主流程禁止 Agent 静默降级：Agent 0/1/2 任一未配置、502/超时、限流、返回格式或覆盖异常时，必须在提交 Image2 前终止，保留配音和字幕供断点续跑。只有 `generation_source = gemini` 的当前版本资料可以在生产流程复用；本地 fallback 只服务于测试或明确的非生产调用。

## 6. 画面和提示词底线

- 默认横版 2:1。
- 默认单一完整画面，避免无意义多格漫画、拼贴、分屏、信息图。
- 都市惊悚强调冷青灰/墨黑、少量暗红、高反差和克制表情，但避免夸张血腥和怪物堆砌。
- 口播科普/情感插画必须明确“插画、绘本、厚涂”等媒介优先级，避免“真实生活场景”被模型误解为摄影写实。
- 人物一致性使用稳定角色 ID、角色卡、状态/服装阶段和参考图绑定。
- Python 应去除提示词中重复人物资料、泛称和冲突描述。
- 参考图最多三张，Agent 只看编号和绑定关系，Image2 才接收实际图片。
- 没上传参考图时必须是纯文生图，不得悄悄调用参考图接口。

### 设备三态规则

1. `none`：无设备要求。
2. `device_interaction`：原文只说看手机、用电脑、收到消息但没有明确内容。画面表现人物使用设备，屏幕不可读、不编造文字。
3. `screen_insert`：原文明示具体短信、照片、信件、网页或屏幕内容。镜头以设备/载体内容特写为主，一般不再混合人物脸部大特写。

设备规则由 Agent 0 识别关键对象、Agent 1 标记镜头模式、Agent 2 写镜头、Python 最终兜底。长语义单元中的多个证据必须按当前固定画面组的字幕重新定位，不能把同一内容泄漏到所有子画面。界面和纸面辅助细节可以自然发挥，只有改变剧情的具体姓名、数字、金额、日期、结论和证据必须以原文为准。

## 7. TTS 关键约束

- IndexTTS2 是主要高质量本地方案，Qwen-TTS 是无高性能显卡时的备选。
- 4090 日常并行建议 2；并行 3 是上限尝试档，不保证所有参考音频和句长都安全。
- 单句过长会明显增加显存峰值，因此动态断句要保留最大长度保护；单句太短又会降低 IndexTTS2 效果，因此要合并短句。
- `“”《》<>【】` 等包裹符号不能单独触发切句。
- TTS 真实进度以临时 WAV 数量为准。
- 用户停止 TTS 时要优雅终止子进程并回收进程树，不能让再次生成一直排队。
- 配音精修必须播放新文件并使用缓存破坏参数；重配后的实际时长要重建整条音频、SRT 和画面时间线。
- 不要为修 Qwen-TTS 破坏 IndexTTS2 已稳定的参数和流程。

## 8. 长文关键约束

- 单次正文上限约 12,000 字符。
- 3,000 字是长文渲染分段目标，不是全文理解边界。
- Agent 0/1 应先拿全文或完整时间线；分段在其后。
- 优先 hard 事件边界，允许略微不均衡，避免拆散同一件事。
- 长文局部 slide ID 和全局 ID 必须有明确映射，画面编辑不得用数组位置猜对应关系。

## 9. 输出和编辑架构

后期一律以 `output/项目名` 为事实来源，不能依赖 `workspace`。标准目录：

```text
input/  原文、配音、参考图、BGM
image/  当前画面
video/  成片
other/  SRT、HTML、映射、Agent 输出、时间线、编辑清单和历史
```

画面修改功能：4 列 × 6 行分页；查看大图；编辑提示词重绘；撤回；重置；本地替换；最多三张参考图；当前图可作为图1；确认单图或全部图为新基线。重绘旧图应备份到该项目 output 子目录。

时序功能：修改画面覆盖字幕范围；移除多余画面并把字幕分给相邻画面；保存当前时序；历史时序；恢复初始时序。

配音精修：一行四句的小卡片、播放/进度、选择、单句或多句重配。

BGM 后期功能：用户即使初次没加 BGM，也能在画面修改项目中添加后直接重渲染。

只有归档清单和必要文件验证通过后才能清理 workspace。

## 10. 渲染关键约束

- 优先 FFmpeg 静态画面直出，Hyperframes 保留兼容回退。
- 字幕版尽量基于纯净版烧录，不重复捕获画面。
- 上方约 5%、下方约 10% 留白是已确认的视觉要求。
- 字幕不能像旧 Bug 那样巨大；默认粗体白字、描边/深色底板、位于下方安全区。
- 某段没有新图时保持上一张图，防止白闪。
- 支持仅字幕版、仅纯净版和双版本；SRT 始终归档。
- BGM 按列表循环，不是单曲循环；每首可调 dB，支持切换/结尾渐弱。

## 11. API 与账号池

- 语言模型支持 Gemini、第三方 OpenAI 兼容、DeepSeek、OpenAI、Kimi、GLM 配置入口。
- 图像模型目前以第三方 Image2/RunningHub 兼容调用为主，UI 对普通用户显示“三方接口”。
- 图像和通用 Key 支持通过加号添加多个账号。
- 多账号应并行或轮询分发；某账号余额不足/鉴权失败后暂时隔离，优先使用仍可用账号，不能头铁重复撞失败账号。
- 重绘多张图也应使用账号池并行。
- API Key 不提交 Git、不写日志明文、不放诊断包。

## 12. 便携部署约束

- 不能使用开发者用户名、绝对盘符或 `%USERPROFILE%` 中的固定缓存路径。
- Python、Node、FFmpeg、浏览器、模型应优先从项目相对目录或显式环境变量解析。
- 普通 Git 仓库不适合提交 17GB 级模型、CUDA 环境、node_modules 和浏览器二进制。Git 维护源码、锁文件、安装脚本、说明和校验；完整整合包通过网盘、Release 或对象存储发布。
- `start_windows.bat` 要识别旧后台；关闭启动窗口后 watchdog 应结束本次服务。
- 用户手动点击运行环境检测，任务启动不做耗时的全量重复检测。

## 13. 安全和 Git 规则

不要提交：

- `.env` 和任何 API Key；
- output、workspace、日志、用户文案、音频、图片、视频；
- Sound Material；
- 模型权重和便携运行时；
- 根目录临时测试文案、压缩包和技术私有记录（除非用户明确要求）。

修改时保留用户已有未提交改动。禁止使用 `git reset --hard` 或随意 checkout 覆盖用户成果。推送前先看 `git status`、`.gitignore` 和暂存内容。

## 14. 近期常见 Bug 历史，避免回归

- 长文多个片段使用重复 slide ID，导致重绘区画面和文案错位。
- 断点续跑重新跑整批 40 张图。
- 重绘带参考图后按钮无响应，或新卡片沿用上一卡片参考图。
- TTS 重配后播放器缓存旧音频。
- BGM 初次没加、后期添加却重渲染不生效。
- 旧后台仍运行导致新修复看似没生效、输出目录打不开。
- 打包后 Hyperframes 使用另一位用户的 Chrome 缓存绝对路径。
- 图像账号 A 没余额后不停重试，账号 B 空闲。
- 字幕过大、安全边距消失、空行和白闪。
- 任务正在跑但 UI 日志为空；停止后后台仍占用队列。
- 项目开始后名称被随机化。
- 插画风提示词因“真实生活场景”“表情真实”等词被模型画成摄影写实。
- 人物描述重复两遍、角色泛称重复，降低提示词清晰度。
- 手机内容不明确时却生成文字特写，明确时又把人脸和屏幕硬塞在同一镜头。
- Agent 0/1 上游失败后静默生成空人物表，Agent 2 再把大量原文直接塞进提示词并继续付费出图。
- 设备证据所在语义单元过长时，同一屏幕、账单或照片被重复注入多张相邻海报。

## 15. 当前验证基线

- 截至 2026-07-31，自动化测试 119 项通过。
- 最近主分支推送过的基线提交为 `903d33d`，但新对话开始时必须以实际 `git log -1` 为准。
- 每次改动至少进行相关 Python 测试；改前端需运行 Vite build；涉及渲染/路径/进程控制时还需要 Windows 实机烟雾测试。

## 16. 新 Codex 接手后的第一轮动作

1. 读取本文件、最新技术架构和默认路线文档。
2. 查看 `git status --short`，确认用户已有改动。
3. 查看当前是否有生成任务、TTS 或渲染进程运行。
4. 按问题定位到 `pipeline.py`、`story_agents.py`、`module4_video_render.py`、`module5_video_render.py`、`visual_editor.py` 或 `App.vue`。
5. 先复现和取证，再最小范围修改。
6. 运行相关测试并说明未覆盖的实机风险。
7. 未经用户要求，不主动提交、推送、清理 output/workspace 或重打整合包。
