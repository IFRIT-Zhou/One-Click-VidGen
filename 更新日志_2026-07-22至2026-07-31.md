# One-Click VidGen 更新日志

更新范围：2026-07-22 至 2026-07-31

本阶段的重点，是把“一条能跑通的自动化流水线”进一步打磨成可以长期使用、反复修改和交付给内测用户的完整视频生产工具。

## 核心功能更新

### 1. 三 Agent 分工与语义分镜升级

- 新增 Agent 0，负责通读校对后的全文，提取故事背景、人物档案、关键物件、地点、线索与连续性规则。
- Agent 1 改为专注时间轴：结合字幕时间戳划分语义单元、事件边界、画面节奏及镜头所需角色。
- Agent 2 继续负责在 Python 固定分组之后撰写每张图的最终生图提示词。
- 长文优先按 Agent 1 的完整事件边界分段；字数上限只作为安全限制，语义完整性优先于最短画面时长。
- 增加硬边界、软边界和节奏提示，减少“同一件事被切成两张图”或承上启下语句错位的问题。
- 专家模式开放 Agent 0、Agent 1、Agent 2 指令编辑，同时保留不可修改的协议与 JSON 结构保护。
- 增加语言模型规划硬门禁：Agent 0、Agent 1 或 Agent 2 任一未配置、超时、限流、返回异常或解析失败时，在提交 Image2 前终止任务；保留配音和字幕供断点续跑，不再用原文兜底继续消耗图像积分。

### 2. 角色、参考图与设备画面控制

- 主界面支持最多三张参考图，按图1、图2、图3编号；未上传时仍保持纯文生图。
- Agent 只接收参考图编号和角色绑定关系，不读取图片像素，避免无意义增加语言模型 token 消耗。
- 画面修改区支持本地参考图、当前画面作为图1、重绘、多账号并行、撤回、重置和确认基线。
- 加强角色 ID、人物状态、服装阶段和参考图绑定，减少角色换脸、换装、丢失帽子或身份混淆。
- 新增设备镜头三态规则：无设备、人物使用设备、设备屏幕特写。只有原文明确给出屏幕内容时才生成屏幕特写；内容不明确时只表现人物正在使用设备。
- 增加单画面构图约束，降低无必要的多格漫画、拼贴和蒙太奇画面比例。
- 修正长设备/证据事件向多个子画面泄漏的问题：每个固定画面组只继承自身字幕真正提到的手机内容、报告、账单或照片，减少相邻海报重复。
- 设备和纸质载体特写改为“核心剧情信息以原文为准，界面与版式可自然发挥”，允许合理状态栏、列表、图标、表格线和留白，不再用绝对禁令锁死画面设计。

### 3. TTS 与字幕链路增强

- IndexTTS2 动态断句升级：兼顾语义、标点、最短长度和安全最大长度；引号、书名号、尖括号、方括号等包裹符号不再被错误当作独立断句依据。
- 配音期间直接监控临时音频文件，显示真实完成句数，减少模型加载阶段日志“像卡死”的感觉。
- 增加安全停止和显存溢出后的清理保护。
- Qwen-TTS 作为无高性能显卡时的云端备选，支持官方音色、配音描述和统一音频规格处理。
- 新增音色试听、整段配音试听和音频下载。
- 新增配音精修：按原始 TTS 断句试听、单选或多选重配，并同步更新整条配音、字幕与画面时间线。
- 单独字幕识别模块支持上传常见音频或视频，使用 Faster-Whisper 识别，并可用原文或语言模型校对，产出独立 SRT。
- 字幕工具增加字幕烧录样式、系统字体选择和独立 BGM 功能。

### 4. 分步模式与任务控制

- 新增分步模式：配音完成后暂停试听，确认后生成画面；画面确认后再继续渲染。
- 支持重新配音、断点续跑、停止生成、删除历史任务和导出问题诊断包。
- 优化停止逻辑，减少停止后长时间占用进程、再次启动一直排队的问题。
- 修复后台任务仍在运行但前端日志为空的问题。
- 启动任务时不再把用户填写的项目名称替换为随机名称。
- 任务完成后增加声音提醒与浏览器注意提醒。

### 5. 画面修改、时序和后期返修

- 画面修改区改为每页 24 张、每行 4 张，按页加载，降低历史长视频的页面压力。
- 支持放大查看、编辑提示词、重绘、撤回、恢复初始提示词、本地替换、参考图和确认当前图为新基线。
- 被替换的旧图保存到项目输出目录内，方便回退和查找。
- 新增画面时序编辑：调整每张画面覆盖的字幕范围、移除多余画面、平均分配相邻字幕、保存当前时序、查看历史时序和恢复初始时序。
- 新增项目级配音精修与 BGM 修改，修改完成后可直接重渲染，不必重新运行 Agent 或重新出图。
- 后期编辑数据统一依赖 `output/项目名/other`，不再依赖易被清理的临时 workspace。

### 6. BGM 与渲染能力

- 主流程、画面修改重渲染和单独字幕工具均支持 BGM。
- 支持不限数量的音乐列表、试听、顺序调整、单首音量、清空列表和列表循环。
- 支持曲目切换及视频结尾渐弱，渐弱时长可调。
- 渲染链路增加 FFmpeg 静态画面直出方案，并保留兼容回退路径。
- 字幕版优先基于纯净版进行字幕烧录，避免重复执行昂贵的画面捕获。
- 恢复画面上方约 5%、下方约 10% 的安全留白及较克制的字幕尺寸。
- 无新画面时保持上一张画面，减少白闪。
- 支持仅纯净版、仅字幕版或双版本输出；无论选择哪种，SRT 都会归档。

### 7. API、多账号与便携运行

- API Key 改为状态卡片：配置成功显示绿色状态，错误显示红色，可通过编辑按钮重新填写。
- 语言模型支持 Gemini、第三方 OpenAI 兼容接口、DeepSeek、OpenAI、Kimi 和 GLM 的配置入口。

### 8. 纯科普模式

- 在都市惊悚、口播科普和通用自定义之外新增“纯科普”模式，面向生物、医学、物理、化学、数学、天文、地学、工程、计算机及人文社科等严肃知识内容。
- 默认人物设定留空，不强制主持人或固定角色；根据学科自动选择结构图、机制图、实验装置、函数图像、地图、时间轴、系统图等视觉语言。
- 专业术语、公式、坐标、年代、地名和必要结构标签不套用普通海报的机械字数限制，但仍要求内容准确、层级清晰且避免把整段旁白塞进画面。
- 图像接口支持多个 API Key，多账号并行分发；余额不足或不可用的账号会暂时跳过，避免反复撞同一个失败账号。
- 增加手动运行环境检测，不再每次生成前强制全量检测。
- 改进便携浏览器、FFmpeg、Python 和模型路径解析，减少写死用户目录的问题。
- 启动脚本能够识别并关闭旧后台；关闭启动窗口时由 watchdog 协助结束本次前后端服务。
- 修复打开项目输出目录无响应的问题。

## 重要 Bug 修复

- 修复长文分段后画面、提示词、字幕和重绘卡片错位。
- 修复断点续跑重新规划全部画面、重复等待返图的问题。
- 修复重绘携带参考图时播放按钮无响应、参考图状态互相污染及预览未更新。
- 修复重配音成功后仍试听旧缓存音频的问题。
- 修复 BGM 后加时重渲染不生效、BGM 清单未归档的问题。
- 修复重渲染按钮状态错误、日志没有进度、停止按钮残留可用的问题。
- 修复单个图像账号余额不足时持续重试而不使用其他可用账号。
- 修复字幕空行、白闪、字幕过大和画面安全边距丢失。
- 修复便携包在其他电脑找不到 Hyperframes 浏览器或错误读取当前用户缓存路径。
- 修复旧后台进程导致新代码未生效、文件夹打开失败或 BGM 配置被忽略。
- 修复图像提示词中的泛化角色称呼、重复人物描述和写实风对插画媒介的覆盖。
- 修复手机、平板、电脑镜头中人物特写与屏幕内容混在一起导致的透视和信息错误。
- 修复 Agent 上游 502/超时后静默生成空人物表、空角色 ID，并把字幕原文直接拼进生图提示词的问题。
- 修复旧的 `local_fallback` Agent 产物在断点续跑时被误当作有效规划继续使用的问题。
- 修复同一长设备语义单元把同一份屏幕或账单证据重复注入多张相邻海报的问题。

## 当前状态

- 主流程、分步流程、字幕工具、画面返修、时序编辑、配音精修和 BGM 重渲染已形成闭环。
- 2026-07-31 当前自动化测试集共 119 项通过。
- 项目仍属于快速迭代中的内测版本；模型服务、第三方 API 审核、显存和网络状态仍会影响单次生成结果。
