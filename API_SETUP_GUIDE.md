# OCV API 配置说明

这份说明适用于不使用 OCV 号池、希望接入自己 API 的用户。配置入口位于左侧 **接口与服务**。

> 不会配置 API，或不确定服务商接口是否兼容时，建议直接使用 OCV 号池。自行配置时，请以你购买 API 的服务商文档为准。

## 一、开始前准备

你通常需要向 API 服务商获取以下信息：

- API Key：调用接口的密钥；
- API Base URL：接口的基础地址；
- 模型 ID：服务商要求填写的模型名称；
- 图像接口用户还需要确认文生图、参考生图和任务查询路径。

API Key 只保存在本机 `.env` 文件中，OCV 页面不会回显密钥原文。请只使用可信服务商，不要把 API Key 发给他人或放进截图、日志、诊断包。

## 二、配置语言模型

OCV 的 Agent 使用语言模型分析文案、规划分镜并整理提示词。

### 方式 A：官方 API

1. 打开 **接口与服务 → 语言模型**；
2. 将 **接口来源** 选择为 **官方 API**；
3. 选择模型家族和 Agent 模型；
4. 点击右下角的 `＋`，填写相应官方 API Key；
5. 点击 **保存语言接口设置**。

当前界面内可直接选择 Google Gemini、DeepSeek 等已适配的官方接口。官方模式不需要自行填写 API Base URL。

使用 DeepSeek 官方 API 时，OCV 默认显式关闭思考模式，以减少推理 token 消耗并提高分镜 JSON 输出的稳定性，无需用户另行设置。

### 方式 B：自定义兼容接口

自定义语言接口必须兼容 OpenAI 的 `/chat/completions` 请求格式，并能稳定输出 JSON。

填写示例：

```text
接口来源：自定义兼容接口（高级）
模型 ID：服务商文档中提供的模型名
API Base URL：https://api.example.com/v1
API Key：服务商提供的密钥
```

注意：

- Base URL 一般填写到 `/v1`，不要把 `/chat/completions` 重复写进去，除非服务商文档明确要求；
- 模型 ID 必须完全照抄服务商文档，注意大小写、斜杠和连字符；
- 本地接口如果确实不需要鉴权，API Key 可以留空；
- 模型需要具备足够的上下文长度，并可靠遵循“输出 JSON”的要求；
- 推理模型可能消耗大量 reasoning tokens，断句等任务更适合稳定的非推理模型。

自定义接口的 **思考模式** 默认为“跟随接口默认”。使用支持 DeepSeek `thinking` 参数的兼容接口时，可以选择“强制关闭”以减少推理消耗；其他模型或不确定兼容性时不要强制设置。

## 三、配置图像模型

进入 **接口与服务 → 图像模型配置**，点击 **新增模型**。每个模型、接口和 Key 独立保存，可以建立多套配置并在新建任务时选择。

基础字段：

```text
配置名称：给自己看的名称，例如“Image 2.5 · 我的接口”
API Base URL：https://api.example.com
模型 ID：服务商文档中提供的模型名
API Key：服务商提供的密钥
支持分辨率：按服务商实际支持情况勾选 1K / 2K / 4K
支持参考图：服务商支持图生图或多图参考时勾选
```

如果服务商给出的接口路径与默认值不同，请展开 **接口路径与能力（高级）**，分别填写：

```text
文生图路径：/openapi/v2/{model}/text-to-image
参考生图路径：/openapi/v2/{model}/image-to-image
状态查询路径：/openapi/v2/query
```

`{model}` 会自动替换成上面填写的模型 ID；也可以直接填写服务商提供的完整路径或完整网址。

### 图像接口兼容要求

OCV 当前接入的是“提交任务后查询结果”的异步出图协议，不是所有标注为 OpenAI 兼容的图像接口都能直接使用。

提交任务时通常需要兼容这些字段：

```json
{
  "prompt": "画面提示词",
  "aspectRatio": "2:1",
  "resolution": "1k",
  "imageUrls": ["可选的参考图地址"]
}
```

提交响应需要返回任务 ID，查询接口需要能够根据任务 ID 返回任务状态、错误信息和最终图片地址。具体字段名和能力以服务商文档为准；如果服务商只提供 `/v1/images/generations`、ComfyUI 工作流或其他私有格式，目前不能保证直接兼容。

保存后，新建任务时可在 **本任务图像模型** 中选择该配置和出图分辨率。图片工作室也会共用这里保存的配置。

## 四、常见填写错误

### 提示“网络异常”或“连接失败”

- 检查 Base URL 是否可以访问；
- 检查是否漏写 `https://`；
- 检查代理、VPN、防火墙或服务商区域限制；
- 本地接口要确认服务已经启动，端口与网址一致。

### 提示 401、403 或无权调用

- API Key 错误、过期或没有对应模型权限；
- 服务商账户余额不足；
- 模型 ID、接口区域或账号套餐不匹配。

### 提示 404 或任务不存在

- Base URL 或接口路径填写错误；
- 文生图、参考生图和查询接口来自不同版本；
- 服务商已经更换或下线接口。

### 提示参数校验失败

- 当前模型不支持所选比例或分辨率；
- 服务商文档与实际接口参数不一致；
- 该接口并不兼容 OCV 当前的异步出图协议。

遇到此类问题，请先保存完整报错和任务 ID，再向 API 服务商确认；不要公开 API Key。

## 五、最简单的排查顺序

1. 对照服务商文档检查 Base URL、模型 ID 和接口路径；
2. 确认 API Key 有余额且具备该模型权限；
3. 先用短文案或图片工作室生成一张测试图；
4. 成功后再运行完整任务；
5. 仍然失败时导出 OCV 诊断包，并在发送前确认其中不包含个人隐私素材。

OCV 不指定或推荐任何第三方中转服务。第三方接口的价格、稳定性、数据处理和售后责任由相应服务商承担。
